
#include <iostream>
using namespace std;

int main(void) {

	int n = 0;
	while(cin >> n && n > 0) {

		bool[][] used = new bool[n][n];
		memset(used, false, sizeof(bool) * n * n);

		int[] pos = new int[n];
		memset(used, 0, sizeof(int) * n);

	}
	return 0;
}